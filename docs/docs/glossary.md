# Глоссарий единого языка

## Доступ

**access:Администратор**
:   (_Administrator_)

    Пользователь, обладающий полномочиями управления системой 
    (создание/изменение пользователей, назначение ролей, управление
    правилами утверждения).


**access:Экспериментатор**
:   (_Experimenter_)

    Пользователь, управляющий экспериментами.


**access:Утверждающий**
:   (_Approver_)

    Пользователь, утверждающий эксперименты.


**access:Наблюдатель**
:   (_Viewer_)

    Пользователь, имеющий право только на просмотр информации в системе.


**access:ID**
:   Строка (UUID), уникальная для каждого пользователя и 
    идентифицирующая его в системе.


**access:IAP ID**
:   Строка, уникальная для каждого пользователя, служащая для входа в аккаунт,
    как описано в [ADR001](adr/001-auth.md).


**access:Группа Утверждающих**
:   Группа из <access:Утверждающий|Утверждающих> и минимальное 
    количество их согласий, необходимое для принятия решения.


**access:Роль**
:   Одна из четырёх ролей: 

    - <access:Администратор>
    - <access:Экспериментатор>
    - <access:Утверждающий>
    - <access:Наблюдатель>

    У одного пользователя может быть только одна роль.


## Эксперименты


**exp:Feature flag**
:   Параметры поведения продукта, которые можно переключать без нового релиза. 
    Флаг состоит из ключа и значения по умолчанию, которое используется, 
    если эксперимент не применяется.


**exp:Ключ флага**
:   Уникальный идентификатор флага. Состоит из `[A-Za-z0-9_-]+`.


**exp:Тип флага**
:   Один из трёх возможных типов значений флага:

    - строка (string)
    - число (number)
    - логическое значение (boolean)


**exp:ID**
:   Уникальный идентификатор эксперимента


**exp:Эксперимент**
:   Временная конфигурация, которая подменяет значение конкретного feature 
    flag для части пользователей с целью проверки гипотез. 
    Эксперимент содержит варианты, таргетинг, метрики, guardrails и 
    проходит через жизненный цикл от черновика до завершения.


**exp:Статус**
:   Одно из возможных состояний эксперимента:

    - Черновик — редактируется.
    - На ревью — ждёт ревью.
    - Одобрен — прошёл ревью, готов к запуску.
    - Запущен — активен, участвует в выдаче значений флага.
    - На паузе — временно остановлен (не выдаёт значение как эксперимент), но сохраняет конфигурацию.
    - Завершён — решение принято.
    - В архиве — для истории.
    - Отклонён — может быть переделан и отправлен заново.


**exp:Версия**
:   Номер версии конфигурации эксперимента, начиная с 1.


**exp:Вариант**
:   Вариант эксперимента — значение, подставляемое попавшему в эксперимент
    пользователю, вместо значения по умолчанию.


**exp:Правило таргетирования**
:   Описанное на формальном языке правило фильтрации пользователей,
    попадающих в эксперимент.


**exp:Итог эксперимента**
:   Один из возможных вариантов с текстовым комментарием:

    - Раскатка победителя (winner rollout)
    - Откат к значению по умолчанию
    - Нет эффекта


**exp:Метрика**
:   Одна из возможных типов метрик эксперимента:

    - целевая — основная метрика, по которой команда оценивает успех эксперимента.
    - вспомогательная
    - аварийная — метрики, по которым выполняются автоматические остановки/откаты.

    Ссылается на <stat:ID Метрики>.


**exp:Конфликтный домен**
:   Определение класса экспериментов, которые не могут быть применены одновременно
    ни при каких обстоятельствах. Выражается в виде уникальной человекочитаемой
    строки (например, "checkout"). Если домен не определён, эксперимент считается
    не вызывающим конфликтов.


**exp:Приоритет**
:   Приоритет эксперимента определяет, насколько он важен, а также определяет
    порядок применения экспериментов при их конкуренции. Выражается числом, 
    притом чем меньше значение, тем выше приоритет. Незаданный приоритет
    считается низшим. 


**exp:Политика разрешения конфликта**
:   Способ разрешения ситуации, когда несколько конфликтующих экспериментов
    претендуют на одного пользователя.


**exp:Решение ревью**
:   Действие, предпринятое уполномоченным для ревью лицом (отклонить/одобрить)
    с метаданными времени и причины принятия решения (в свободной форме).
    Хранится для обеспечения прозрачности решений и дальнейшего аудита.


**exp:Статус решения**
:   Один из двух возможных типов решения ревью:

    - одобрить
    - отклонить


## Выдача


**rt:Субъект**
:   Некто (пользователь или устройство), запрашивающий конфигурацию Feature flags.

**rt:ID Субъекта**
:   Стабильный идентификатор субъекта (пользователя или устройства), для которого 
    принимается решение.


**rt:Атрибуты субъекта**
:   Набор характеристик (страна, версия приложения, платформа), 
    передаваемых в момент запроса для работы таргетинга.


**rt:Решение**
:   (_Decision_)

    Конкретное решение о выдаче субъекту тех или иных значений флагов.


**rt:ID Решения**
:   Уникальный идентификатор конкретного решения о выдаче флага. 
    Связывает показ варианта с последующими событиями.


**evt:ID**
:   Уникальный идентификатор события.


## События


**evt:Событие**
:   (_Event_)
    Факт взаимодействия пользователя с продуктом (клик, показ, покупка).
    Обязательно принадлежит одному из Типов событий.


**evt:ID Типа**
:   Уникальный идентификатор типа события.


**evt:Тип события**
:   Определение класса событий и их метаданных.


**evt:Схема события**
:   Машиночитаемое описание структуры полей (ключи и типы значений) типа события.


**evt:Политика атрибуции**
:   Требование (или его отсутствие) наличия в рамках данного решения уже произошедшего
    другого события. Например, событие "Клик" требует событие "Показ".


**evt:Статус**
:   Статус обработки события может принимать одно из следующих значений:

    - ожидает атрибуцию
    - принят


## Метрики и отчёты


**stat:ID Отчёта**
:   Уникальный идентификатор отчёта.


**stat:Отчёт**
:   Определение для одного эксперимента набора метрик, которые рассчитываются в
    заранее заданном временном окне.


**stat:ID Метрики**
:   Уникальный идентификатор метрики. Удовлетворяет regex `[A-Za-z0-9_-]+`


**stat:Метрика**
:   Определение метрики — числового показателя, вычисляемого посредством агрегации
    определённых событий.


## Guardrail


**guard:ID Правила**
:   Уникальный идентификатор правила.


**guard:Правило**
:   Определение метрики, окна наблюдения, порога срабатывания и результирующего действия,
    необходимого для предотвращения деградации вследствие эксперимента.


**guard:Журнал аудита**
:   Совокупность записей аудита.


**guard:ID Записи**
:   Уникальный идентификатор записи в журнале аудита.


**guard:Запись аудита**
:   Содержит подробную информацию о срабатывании <guard:Правило|Правила> для
    дальнейшего анализа причин провала.


## Оповещения


**notif:ID Правила**
:   Уникальный идентификатор правила оповещения.


**notif:Канал связи**
:   Какая-либо интеграция с внешней системой сообщений (Telegram, Slack, Discord, Email, и т.д.)


**notif:Правило оповещения**
:   Описывает, при каких условиях будет выпущено оповещение, как оно будет выглядеть
    и по каким каналам связи будет доставлено.


**notif:Строка подключения**
:   Описывает канал связи, по которому будет отправляться оповещение.

    См. ADR003 и [Спецификацию строк подключения](spec/notification-dsl.md).


**notif:Триггер оповещения**
:   Описывает, в каком случае должно сработать оповещение. Варианты:

    - обновление статуса эксперимента
    - срабатывание определённого guardrail

    См. ADR003 и [Спецификацию триггеров](spec/notification-dsl.md).


**notif:Шаблон**
:   Шаблон текста оповещения. Описывается на языке шаблонизации Jinja.


**notif:Ratelimit**
:   Лимит периода срабатывания правила оповещения в секундах (раз в сколько секунд
    можно отсылать).


**notif:Подготовленное оповещение**
:   Оповещение, выпущенное, но ещё не отправленное. См. ADR004.


**notif:Отпечаток**
:   (_Fingerprint_) хэш выпущенного оповещения для дедупликации. См. ADR004.


## Разрешение конфликтов


**conflict:Конфликт**
:   Ситуация, при которой несколько экспериментов из 
    <exp:Конфликтный домен|Конфликтного домена>
    доступны для применения на одном субъекте.


**conflict:Разрешение конфликта**
:   Действие, предпринятое в соответствии с 
    <exp:Политика разрешения конфликта|Политикой разрешения конфликта>
    и зафиксированное в журнале конфликтов.


**conflict:Журнал конфликтов**
:   Совокупность записей о разрешении конфликтов.


**conflict:Запись о разрешении**
:   Содержит информацию о домене, в котором произошёл конфликт, 
    <exp:ID> эксперимента, по которому было принято решение, само решение
    (применён, отклонён) и способ (политика) разрешения.


**conflict:Один или никто**
:   Политика разрешения конфликта, при которой отклоняются все эксперименты,
    вступившие в конфликт. Подходит, когда нельзя выбрать один эксперимент
    по причинам нерепрезентативности. В случае такого конфликта возвращается
    значение по умолчанию. См. ADR007.


**conflict:Наивысший приоритет**
:   Политика разрешения конфликта, при которой выбирается эксперимент с
    наибольшим приоритетом (наименьшим его числовым значением). См. ADR007.